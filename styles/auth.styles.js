import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  title: {
    fontSize: 40,
    fontWeight: "bold",
    color: "#333",
  },
  link: {
    fontSize: 18,
    color: "#007bff",
  },
  
  button: { padding: 12, backgroundColor: "#eee", borderRadius: 8, marginTop: 16 },
});
